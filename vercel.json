{
  "buildCommand": "npm install --legacy-peer-deps && npm run build",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "nextjs",
  "functions": {
    "app/api/**/*.js": {
      "maxDuration": 30
    }
  },
  "env": {
    "NODE_ENV": "production",
    "QUEUE_ENABLED": "false"
  },
  "build": {
    "env": {
      "NODE_ENV": "production",
      "QUEUE_ENABLED": "false"
    }
  }
}
